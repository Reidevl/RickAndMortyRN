import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  title: {
    marginBottom: 8,
  },
  airDate: {
    marginBottom: 4,
  },
  episodeCode: {
    marginBottom: 12,
  },
  episodeCharacters: {
    marginBottom: 8,
  },
});