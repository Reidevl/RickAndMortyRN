import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    marginBottom: 8,
    gap: 8,
  },
  name: {
    marginBottom: 8,
    textAlign: "center",
  },
  label: {
    fontWeight: "bold",
  },
});
